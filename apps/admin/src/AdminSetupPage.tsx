import { FormEvent, useState } from 'react'
import { bootstrapDemoAdmin, setAdminToken } from './api'
export function AdminSetupPage(){
 const [form,setForm]=useState({name:'',email:'',password:''});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [done,setDone]=useState(false)
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await bootstrapDemoAdmin(form);setAdminToken(r.token);setDone(true)}catch(e){setError(e instanceof Error?e.message:'Não foi possível criar o administrador.')}finally{setBusy(false)}}
 if(done)return <main className="admin-login"><section className="login-card"><span className="eyebrow">ACESSO ATIVADO</span><h1>Administrador criado</h1><p>Sua conta individual está ativa. As novas ações administrativas poderão ser vinculadas ao seu usuário.</p><button className="primary" onClick={()=>location.hash='visao-geral'}>Ir para o Admin</button></section></main>
 return <main className="admin-login"><section className="login-card"><div className="brand login-brand"><span className="brand-mark">A</span><div><strong>APETIT</strong><small>Admin</small></div></div><span className="eyebrow">CONFIGURAÇÃO INICIAL</span><h1>Criar primeiro administrador</h1><p>Defina sua própria conta. A senha não será exibida novamente nem armazenada em texto legível.</p><form onSubmit={submit}><label><span>Nome</span><input required minLength={2} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label><span>E-mail</span><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label><span>Senha</span><input required type="password" minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="alert error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Criando...':'Criar administrador'}</button></form></section></main>
}
