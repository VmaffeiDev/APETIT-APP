# Railway auto-deploy check

Arquivo inofensivo criado para validar que o serviço `api-staging-v2` acompanha automaticamente a branch `foundation/v1` quando há mudanças em `backend/**`.

Pode ser removido depois da validação.
